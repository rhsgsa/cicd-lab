apiVersion: v1
kind: Secret
metadata:
  name: registry-credentials
  annotations:
    tekton.dev/docker-0: https://${REGISTRY_HOSTNAME}
    tekton.dev/docker-1: http://gitea.${GIT_PROJ}.svc:3000
type: kubernetes.io/basic-auth
stringData:
  username: userX
  password: ${STUDENT_PASSWORD}
